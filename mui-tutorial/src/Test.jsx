import React from "react";
import { Typography } from "@mui/material";

const Test = () => {
  return (
    <Typography variant="h1" sx={{ color: red }}>
      Hello World
    </Typography>
  );
};

export default Test;
